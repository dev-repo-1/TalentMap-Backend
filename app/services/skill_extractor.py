"""Skill seeding from declared names (stub — full ESCO linking ships later)."""

from sqlalchemy.ext.asyncio import AsyncSession


async def seed_skills_from_declared(_db: AsyncSession, _employee_id: str, _declared_skills: list[str]) -> None:
    return None
